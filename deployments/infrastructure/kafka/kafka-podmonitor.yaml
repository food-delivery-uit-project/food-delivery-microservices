apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: kafka-exporter
  namespace: kafka
  labels:
    app: kafka-exporter
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: kafka-exporter
  podMetricsEndpoints:
  - port: tcp-prometheus
    path: /metrics
